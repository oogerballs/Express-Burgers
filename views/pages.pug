extends layout

block content
    div
        p This is the #{title} page
    
    body
        div
            ul
                each item in config.food[page]
                    a(href=item[3])
                        div(class="food_item")
                            img(src=item[3], alt=item[2], class="image")
                            //- div(class="slider")
                            //-     div(class="text")
                            span #{item[0]}